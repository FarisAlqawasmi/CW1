# Productivity Tracker REST API

A simple RESTful API for managing tasks and habits. Create, read, update, and delete tasks (with due date, priority, status) and habits (with frequency, streak, and daily completion) via JSON over HTTP.
